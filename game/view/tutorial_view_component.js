var tutorialViewComponent = function() {
	var my = {};

	var init = function(renderSystem, entity, eventManager) {

	};

	var render = function(renderSystem, entity, eventManager) {

	};

	return viewComponent(init, render);
};